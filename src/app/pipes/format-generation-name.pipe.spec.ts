import { FormatGenerationNamePipe } from './format-generation-name.pipe';

describe('FormatGenerationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGenerationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
