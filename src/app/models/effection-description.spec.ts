import { EffectionDescription } from './effection-description';

describe('EffectionDescription', () => {
  it('should create an instance', () => {
    expect(new EffectionDescription()).toBeTruthy();
  });
});
