import { EncounterMethodRates } from './encounter-method-rates';

describe('EncounterMethodRates', () => {
  it('should create an instance', () => {
    expect(new EncounterMethodRates()).toBeTruthy();
  });
});
