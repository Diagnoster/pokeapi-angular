import { DamageCategoryColor } from './damage-category-color';

describe('DamageCategoryColor', () => {
  it('should create an instance', () => {
    expect(new DamageCategoryColor()).toBeTruthy();
  });
});
