export class MapLocation {
    constructor(
        public id: number,
        public name: string,
        public url: string,
        public img: string,
        public version_groups: string
    ) { }
}
